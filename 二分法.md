什么时候需要使用二分法：
1.最少是多少，最多是多少
2.直接求很难，验证很容易
4.答案具有单调性

向上取整
(a + b - 1) // b（要求 a ≥ 0，b > 0）

binary search 用的主要是双指针检索：
回顾：
双指针的边界条件一般为l <= r（或l < r）
滑动窗口的边界条件一般为r < len(str)（不是 len(str)-1！）

求中间值middle= l + ((r - l) // 2)(永远不会溢出)
(l + r) // 2(可能会溢出，但是需要数很大)


传统的双指针题目
Binary Search

Search a 2D Matrix
